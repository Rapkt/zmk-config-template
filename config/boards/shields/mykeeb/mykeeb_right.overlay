#include <dt-bindings/zmk/matrix_transform.h>

/ {
    // This tells ZMK how your 42 keys are arranged across both halves
    default_transform: keymap_transform_0 {
        compatible = "zmk,matrix-transform";
        columns = <12>; 
        rows = <4>;
        
        // The offset belongs here!
        col-offset = <6>; 
             
        map = <
            RC(0,0) RC(0,1) RC(0,2) RC(0,3) RC(0,4) RC(0,5)     RC(0,6) RC(0,7) RC(0,8) RC(0,9) RC(0,10) RC(0,11)
            RC(1,0) RC(1,1) RC(1,2) RC(1,3) RC(1,4) RC(1,5)     RC(1,6) RC(1,7) RC(1,8) RC(1,9) RC(1,10) RC(1,11)
            RC(2,0) RC(2,1) RC(2,2) RC(2,3) RC(2,4) RC(2,5)     RC(2,6) RC(2,7) RC(2,8) RC(2,9) RC(2,10) RC(2,11)
                                    RC(3,3) RC(3,4) RC(3,5)     RC(3,6) RC(3,7) RC(3,8)
        >;
    };

    chosen {
        zmk,kscan = &kscan0;
        zmk,matrix_transform = &default_transform; // Connects the matrix map
    };

    kscan0: kscan_0 {
        compatible = "zmk,kscan-gpio-matrix";
        diode-direction = "col2row";

        // Reversed columns to match your hardware testing
        col-gpios
            = <&gpio0 9  GPIO_ACTIVE_HIGH> 
            , <&gpio0 10 GPIO_ACTIVE_HIGH> 
            , <&gpio1 11 GPIO_ACTIVE_HIGH> 
            , <&gpio1 13 GPIO_ACTIVE_HIGH> 
            , <&gpio1 6  GPIO_ACTIVE_HIGH> 
            , <&gpio0 24 GPIO_ACTIVE_HIGH> // Dummy pin for the 6th column
            ;

        // Corrected rows to match your hardware testing
        row-gpios
            = <&gpio0 31 (GPIO_ACTIVE_HIGH | GPIO_PULL_DOWN)> // Row Top
            , <&gpio0 15  (GPIO_ACTIVE_HIGH | GPIO_PULL_DOWN)> // Row Mid 15
            , <&gpio1 29 (GPIO_ACTIVE_HIGH | GPIO_PULL_DOWN)> // Row Bot 29
            , <&gpio0 2 (GPIO_ACTIVE_HIGH | GPIO_PULL_DOWN)> // Row Thumb 2
            ;
    };
};
